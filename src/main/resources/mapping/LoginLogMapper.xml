<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.publicordercasemanagementsystem.mapper.LoginLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into login_logs (
            user_id, name, ip, login_time, login_result, device_type, browser, os
        ) values (
            #{userId}, #{name}, #{ip}, #{loginTime}, #{loginResult}, #{deviceType}, #{browser}, #{os}
        )
    </insert>

</mapper>
